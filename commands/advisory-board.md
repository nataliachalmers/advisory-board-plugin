---
description: Ask your local podcast advisory board a question
---

Use the `advisory-board` skill to answer the following question. Run `ask.py` at
`$ADVISORY_HOME` (default `~/AdvisoryBoard`), then present each voice's perspective,
where they agree or disagree, and the specific episodes + timestamps to go listen to.
Do not invent anything beyond what the tool returns. If the system isn't installed
yet, tell the user to run `/advisory-board-setup` first.

Question:

$ARGUMENTS
